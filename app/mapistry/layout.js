export const metadata = {
  title: "Mapistry Environmental API",
  description: "Fake Mapistry API for environmental data integration practice",
};

export default function MapistryLayout({ children }) {
  return children;
}
