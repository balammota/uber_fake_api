export const metadata = {
  title: "Mapistry App",
  description: "Mapistry environmental compliance platform simulation",
};

export default function MapistryAppLayout({ children }) {
  return children;
}
