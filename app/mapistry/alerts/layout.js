export const metadata = {
  title: "Mapistry Alerts",
  description: "Mapistry alerts and request logs dashboard",
};

export default function MapistryAlertsLayout({ children }) {
  return children;
}
